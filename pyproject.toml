[project]
name = "robertomaluco-v2"
version = "0.1.0"
description = "Minimal Slack conversational bot"
requires-python = ">=3.11"
dependencies = [
  "slack-bolt==1.22.0",
  "pydantic==2.10.6",
]

[tool.uv]
package = false
